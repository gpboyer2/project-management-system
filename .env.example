# ========================================
# 环境变量配置模板
# ========================================
# 使用方法：
#   1. 复制此文件为 .env（开发）或 .env.prod（生产）
#   2. 根据实际环境修改配置值
#   3. .env 文件不要提交到代码仓库

# 运行环境: development | production | test
NODE_ENV=development

# 认证绕过（生产环境必须设为 false）
BYPASS_AUTH=false

# 测试模式下的账号凭证（仅当 BYPASS_AUTH=true 时有效）
TEST_USERNAME=admin
TEST_PASSWORD=admin123

# JWT 密钥（生产环境必须修改）
# 生成方法: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
JWT_SECRET=your-secret-key-here
JWT_EXPIRES_IN=24h
REFRESH_TOKEN_EXPIRES_IN=7d

# 前后端 Http（运行时注入，必须以 VITE_ 开头）：几个端口相同，用途不同，请勿删除任一
VITE_API_PORT=9200 # 后端服务端口
VITE_API_HOST=localhost # 后端服务主机
VITE_API_BASE_URL=http://localhost:9200/api

# 前端静态服务端口
VITE_FRONTEND_PORT=9300 

# 前后端 WebSocket 配置：几个端口相同，用途不同，请勿删除任一
VITE_WS_PORT=9210           # websocket监听端口（server/etc/config.js 读取）
VITE_WS_HOST=localhost      # websocket服务主机
VITE_WS_URL=ws://localhost:9210   # 前端连接地址（浏览器连接用）

# 日志配置
# 日志级别: debug | info | warn | error
LOG_LEVEL=info
LOG_PATH=./logs

# 文件上传配置
MAX_FILE_UPLOAD_SIZE=10485760
UPLOAD_ALLOWED_EXT=.json,.xml,.txt,.csv
