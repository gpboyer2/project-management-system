
要求你首先阅读理解咱们这个项目的交互功能和展示，特别是功能点和业务点。

我先给你讲讲我的核心目的，以下的所有内容都是基于这个核心目的的。如果与核心目的冲突，以核心目的为准。
我的核心目的是: 在拓扑图页面分化出来一个功能，这功能就是说它是一个下拉列表(或者是一个 tab 按钮按钮组也可以)，它可以切换显示软件视图和协议视图，基于现有的拓扑图页面进行分化,扩展,和优化. 协议视图是浓缩版全局概览，聚焦协议整体流转（入 / 出、解析 / 序列化），内部配置已固化，无需节点调色板，也不用拖拽节点，核心是直观呈现协议完整链路而非具体配置细节。你完全不要考虑节点调色板及拖拽逻辑，只保留协议流转的浓缩展示,  用户想看的是已经配置好的完整通信协议的运行状态和宏观流程，而不是去设计或搭建协议 。软件视图核心是直观图形化全局关联，一眼看清层级归属，内部配置已固化，彻底移除左侧体系层级树 + 画板拖拽逻辑，只保留纯图形化关联展示，无冗余结构, 用户想看的是直观的图形化拓扑关系，一眼就能看出谁是谁的谁。

用户选择软件视图，就是为了看中间那个直观的拓扑图

这两个视图都只有一个中间画布和一个右侧面板。
关键区别：
  - 软件视图：关注系统架构（项目-设备-软件的层级关系）, 看"谁是谁的谁"的系统架构关系（拓扑图）
  - 协议视图：关注通信流程（数据如何从一种协议转换到另一种协议） , 看"数据如何流动"的通信协议关系（协议流程图）
共同点：
  - 都是为了提供直观的可视化展示
  - 都不是为了让用户去编辑或搭建
  - 都是展示已经配置好的内容
用户想通过协议视图快速了解：
  - 当前系统运行了哪些通信协议
  - 每个协议的处理流程和数据流向
  - 协议之间的依赖关系
  - 协议的运行状态和性能指标

------
你全部理解后, 需要你先为我输出一个原型设计图来，我要我想看看你的想法。用 HTML 加 CSS 编写(目录名为 /design/{Date.now()} )，我直接双击打开html查看。


角色定位：你是一位专业的产品专家和UX设计师，精通数据可视化、交互设计和现代Web技术栈。

真实业务场景：
本项目是面向真实军工测控通信的可视化监控系统，所有内部逻辑和业务配置已在其他页面完成，此页面专注于直观展示已配置好的系统架构和通信流程。

核心目的：
在拓扑图页面分化出来一个功能，通过Tab按钮组切换显示软件视图和协议视图，基于现有拓扑图页面进行分化、扩展和优化。

两个视图的正确理解：
1. 软件视图：体系层级视图 - 从真实军工系统的项目/设备/软件的组织架构进行切入
2. 协议视图：通信流程视图 - 从通信协议的宏观流程进行切入

### 用户想看的（必须深刻理解）：

#### 协议视图：
- **严禁节点调色板**：用户想看的是已经配置好的完整通信协议的运行状态和宏观流程
- **严禁拖拽逻辑**：内部配置已固化，不需要用户去设计或搭建协议
- **浓缩版全局概览**：聚焦协议整体流转（入/出、解析/序列化）
- **核心价值**：直观呈现协议完整链路而非具体配置细节

#### 软件视图：
- **严禁拖拽逻辑**：内部配置已固化，只保留纯图形化关联展示，无冗余结构
- **核心价值**：直观图形化全局关联，一眼看清层级归属
- **中间画布**：核心展示！项目-设备-软件的图形化拓扑关系图，让用户一眼看懂"谁是谁的谁"的系统架构

### 共同设计原则：
- **纯展示性**：都不是为了让用户去编辑或搭建，都是展示已经配置好的内容
- **2栏布局**：中间画布 + 右侧面板
- **无冗余信息**：左右侧面板不重复中间画布的核心信息
- **直观可视化**：
  - 软件视图：看"谁是谁的谁"的系统架构关系（拓扑图）
  - 协议视图：看"数据如何流动"的通信协议关系（协议流程图）

### 用户交互：
- 鼠标悬停：节点高亮 + 基本信息
- 单击左键：选中节点 + 右侧详情面板
- 双击左键：跳转关联内容
- 右键点击：上下文操作菜单

### 业务价值：
- **软件视图**：用户能从系统架构角度理解整个军工体系的组织结构和部署关系
- **协议视图**：用户能快速了解当前系统运行了哪些通信协议、每个协议的处理流程和数据流向、协议之间的依赖关系、协议的运行状态和性能指标

------


在你绘制页面的时候，我有几个要求。第一个就是中间那个板块，也就是两种视图中间都有那种画布嘛，要美观、充实。你可以看看，造一些符合我们项目的假数据。这个文案啊，要选对啊，文案要写的对。这是我对你的要求。


